/* @arquivo global.js 
 * @descricao Variaveis globais do sitio
 */

window.Colecao = { }; // Coleções 

window.Modelo = { };  // Modelos

window.Roteador = { }; // Roteador

window.sitio = null;   // Sitio

window.Global = { };   // Conteúdo global.