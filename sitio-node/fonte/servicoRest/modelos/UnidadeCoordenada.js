'use strict';

module.exports = {
  nome: 'UnidadeCoordenada', // Isso deve ser o mesmo nome dado a tabela no banco de dados.
  rotas: ['/coordenadas', '/coordenadas/:id'] // Rotas para o serviço REST.
}